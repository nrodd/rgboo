Place exported scene artwork here. Files are served at `/scene/<filename>`.

Register artwork and its placement in `src/scene/scene.config.ts`.
See `docs/scene-development.md` for the scene and video setup.
