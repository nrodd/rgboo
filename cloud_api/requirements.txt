Flask==2.3.3
gunicorn==21.2.0
google-cloud-firestore>=2.11,<3.0
better-profanity==0.7.0
python-dotenv==1.0.0
# zoneinfo needs a tz database, and python:3.12-slim is not guaranteed to
# ship one. Stats bucket days in STATS_TIMEZONE, so a missing db is a hard
# failure rather than a wrong-by-a-few-hours one.
tzdata==2025.2
